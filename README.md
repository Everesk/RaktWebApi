# RaktWebApi
Практикум, спринт 1
Шундерюк Михаил

Простое ASP.NET Core Web API приложение для управления событиями.

Проект реализован в рамках учебного спринта и демонстрирует базовую архитектуру Web API: разделение слоёв, валидацию, DI и работу со Swagger.

Приложение позволяет:
	получать список всех событий;
	получать событие по идентификатору;
	создавать новое событие;
	обновлять существующее событие;
	удалять событие.

Данные хранятся в памяти приложения (без базы данных).

Стек:
	.NET 9
	ASP.NET Core Web API
	Swagger

Структура проекта
	Controllers — HTTP эндпоинты
	Models — сущности и DTO
	Services — бизнес-логика
	Mappers — преобразование DTO и сущностей
	Extensions — конфигурация приложения

Требования
	установлен .NET SDK 9.0


Запуск проекта
	Клонировать репозиторий:
	git clone <ссылка-на-репозиторий>
	Перейти в папку проекта:
	cd RaktWebApi/RaktWebApi
	Запустить приложение: 
		1) Только http:  dotnet run  
		2) http и https: dotnet run --launch-profile https


После запуска в консоли будут выведены адреса:
http://localhost:5007
https://localhost:7130

Swagger

Swagger доступен только в режиме разработки по адресу: /swagger
Через Swagger можно просматривать и тестировать все эндпоинты.

API

	GET /events
	Получить список всех событий
	Ответ: 200 OK
###

	GET /events/{id}
	Получить событие по идентификатору
	Ответы:
	200 OK — если найдено
	404 Not Found — если не найдено
###

	POST /events
	Создать новое событие
	Content-Type: application/json

	Пример тела:
	{
	"title": "Встреча команды",
	"description": "Обсуждение задач",
	"startAt": "2026-03-25T10:00:00",
	"endAt": "2026-03-25T11:00:00"
	}

	Ответ: 201 Created
###

	PUT /events/{id}
	Обновить событие
	Content-Type: application/json

	Пример тела:
	{
	"title": "Обновленная встреча",
	"description": "Новая повестка",
	"startAt": "2026-03-25T12:00:00",
	"endAt": "2026-03-25T13:00:00"
	}

	Ответы:
	204 No Content — успешно
	404 Not Found — если не найдено
###

	DELETE /events/{id}
	Удалить событие

	Ответы:
	204 No Content — успешно
	404 Not Found — если не найдено
###

Валидация. Реализованы следующие проверки:

	Title обязателен
	StartAt обязателен
	EndAt обязателен
	EndAt должен быть позже StartAt

	При ошибке возвращается 400 Bad Request.


Тестирование

Можно использовать:
	Swagger
	файл RaktWebApi.http

